<%= form_for(tour_status) do |f| %>
  <% if tour_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tour_status.errors.count, "error") %> prohibited this tour_status from being saved:</h2>

      <ul>
      <% tour_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
